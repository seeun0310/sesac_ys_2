<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>파일 업로드 실습</title>
</head>
<body>
    <form action="/upload" method="post" enctype="multipart/form-data" name="register">
        <input type="text" name="id" placeholder="아이디" /><br/>
        <input type="text" name="pw" placeholder="비밀번호" /><br/>
        <input type="text" name="name" placeholder="이름" /><br/>
        <input type="text" name="age" placeholder="나이" /><br/>
        <input type="file" name="userfile" /><br/><br/>
        <button type="submit" onclick="regis()">회원가입</button>
    </form>

    <script>
        function regis() {
            const form = document.forms["register"];
            const data = {
                id: form.id.value,
                pw: form.pw.value,
                name: form.name.value,
                age: form.age.value,
                file: form.username.value
            }

            axios({
                method: 'post',
                url: '/upload',
                data: data
            }).then((res)=> {
                const res = res.data;
                console.log(res.data);
            })
        }
    </script>
</body>
</html>